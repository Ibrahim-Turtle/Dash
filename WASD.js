document.addEventListener('keydown', function(event) {
    var keyCode = event.keyCode;
    if (keyCode === 87) { // W-toets
        // Verplaats de auto naar voren
    } else if (keyCode === 65) { // A-toets
        // Verplaats de auto naar links
    } else if (keyCode === 83) { // S-toets
        // Verplaats de auto naar achteren
    } else if (keyCode === 68) { // D-toets
        // Verplaats de auto naar rechts
    }
});
